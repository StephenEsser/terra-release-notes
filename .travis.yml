language: node_js
sudo: false
cache: false
branches:
  only:
  - master
script:
- npm run build
- npm test
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master
